# Recommendation

1. **A10 is now a ratification question, not an engineering one.** If and when person categories are
   wanted, the polity ratifies PERSON_CATEGORY entries with definitions; the machinery already
   refuses everything else. Recommended: do NOT populate it for the land enterprise. Point the
   instrument at operations.
2. **Answer the anti-mimicry authority fork** (umbrella Pending Decisions) - unchanged as the single
   highest-value open item; everything downstream inherits it.
3. **Run the decision-elicitation probe** (needs the user, ~1hr, no code).
4. **Fix the polarization threshold** - still outstanding from the prior run. 0.900 was derived at
   panel_size=16 and applied as a constant; at n=50 the best separating threshold is ~0.82, so the
   fixed value MISSES escalations (the unsafe direction). Drafted fix: derive per panel size.
5. **Consider extending the vocabulary to state variables** - `StateVar.name` is another free string,
   though a lower-stakes surface than instruments or classes.
6. Uncommitted: all prior files plus three increments.
