# Recommendation - after run 3

1. **F10 `aigov-adversarial`** - highest value now. The kernel exists; the question is whether its
   refusals survive attack (agenda reordering, metric gaming, preference falsification, emergency abuse).
   Every named attack must end in detection or escalation, never silent success.
2. **F9 `aigov-audit-arbitration`** - D15 must be structurally independent of the kernel, or the
   "generate != decide != verify" separation is nominal. Also needed to authenticate ratification records.
3. **H4 (open, kernel):** can the non-steering certifier's domain widen beyond `crop_area_allocation`
   without inventing a check? If not, say so permanently - the governor acts on one instrument.
4. **C2 namespace migration** - retire the `aigov/choice/__init__.py` path shim.
5. **F11 integration** - the phase-1 terminal - needs F9 + F10 first.
6. Still open from earlier runs: quote-verification of the five memos; polarization threshold at other
   panel sizes (H4 intake); the 25% camp-floor value question (H5 intake, user's call).
