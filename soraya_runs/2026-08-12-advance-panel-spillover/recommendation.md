# Recommendation

1. **Run the decision-inventory session** (needs the user, 1-2 hrs). Now materially better than it
   was this morning: it refuses to chain, refuses to group on spreadsheet facts, refuses to source a
   compound question, and asks about checking separately from ownership.
   Protocol: `docs/decision-inventory-protocol.md`.
2. **The coverage-lens criterion needs a REDESIGN, not a patch.** The panel showed the rule
   constrains lens NAMES while mimicry lives in the SET, the PARTITION, and the PREDICATE. A fix has
   to constrain how the object domain is carved and what the lens asks - much harder than the
   version I proposed. Do not build item 8 against the current criterion.
3. **Run /idea-anchor** with `docs/idea-anchor-prompt-v3-2026-08-11.md` (user-confirmed skill).
4. **The two-seat panel is live and money-free** (`scripts/run_panel.py`). Use it on arguments you
   can verify by inspection, not for verdicts you have to trust - two seats agreeing is unanimity,
   not a majority, and both models share training priors.
5. **Commit.** Nothing committed since the public push; this is now six increments outstanding.
6. A10 stays authority-gated by design.
