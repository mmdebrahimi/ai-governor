# Recommendation - after the V14 invariant landing

1. **Answer the anti-mimicry fork** (umbrella Pending Decisions). Does the instrument ever propose
   an institutional FORM, or only a problem + measurable gap? It contradicts the anchor's
   ontology-induction framing and everything downstream inherits the answer. Soraya's drafted
   position is NO - a hard rail - in `docs/idea-anchor-prompt-v3-2026-08-11.md`.
2. **Run the decision-elicitation probe** (anchor 5). One real operational decision, run by hand
   through decision -> statistic -> observation -> instrument. Needs the user; costs an hour.
3. **Refresh the stale candidate-action tables.** Every family's `### Candidate next actions` still
   holds seeding-era work that is already done, so `advance_ranker` cannot rank meaningfully. This
   is the cheapest way to make the code-owned gate informative again.
4. **Resolve the V13 register contradiction** (V14 section 9) as a recorded decision - drafted:
   registers are exempt from the derivation axis, bounded on the retention axis.
5. **Consider extending I12/I13 coverage** from declared classifications to anything the kernel
   applies to a person-shaped subject, which would close the author-declaration residual.
6. Five files remain uncommitted plus this increment.
