# Audit trail - soraya --advance, 2026-08-11 (ratified vocabulary)

Gate mode money-only; every step `auto`. Pre-planned ~35 steps, ran ~45 (self-extended once, when
the rule-target mechanism held and the instrument catalogue became reachable in the same run).

Entry point: an adversarial review of the meaning-channel plan produced a sequenced recommendation
- migrate the rule-target class surface FIRST, close A8 BEFORE the catalogue, and add no charter
clause until an invariant fires mechanically. This run executed that sequence.

| # | Step | Outcome |
|---|---|---|
| 1 | Lock + classify | all planned steps `auto` |
| 2 | Close A8 vector (a): deep-copy specs at admission | caller can no longer alias an admitted registry |
| 3 | Close A8 vector (b): re-validate registry in `run_cycle` | validation is a per-cycle precondition, not a historical claim |
| 4 | **Verify which vectors actually closed** | run_cycle DETECTS; bare `propose()` does not re-validate - stated as a pinned boundary rather than glossed |
| 5 | New artifact `aigov/vocabulary.py` | content-addressed, own `ratified_by` on the kernel's token rail, definitions mandatory |
| 6 | Invariant I15 + charter C37 | rule target classes must resolve to a ratified entry |
| 7 | INSTRUMENT catalogue + invariant I8c + charter C38 | the catalogue fixes the class; the department names a key |
| 8 | **A9 attack re-run against live specs** | both vectors caught: invented instrument, and relabelled catalogued instrument |
| 9 | PERSON_CATEGORY declared RESERVED, entries REFUSED | A10 deliberately NOT closed - entries are an authority decision |
| 10 | Fix stale ontology schema table | `iclass` had never been documented at all |
| 11 | 4 mutation proofs + full regression | each mutant RED; root 257; organ 193/193 |

## Self-extension (auditable, not silent)
Planned to stop after the rule-target surface. Extended when step 8 showed the same mechanism closed
A9 without touching the `Instrument` dataclass - a materially smaller change than the "resolve iclass
from a catalogue" refactor the review had sketched, so the risk that justified deferring it was gone.

## Terminal
Remaining code-closable work is A10, and it is blocked BY DESIGN: the mechanism exists, but populating
PERSON_CATEGORY is a ratification decision, not Soraya's. Everything else on the frontier is the
anti-mimicry authority fork or the user-bound elicitation probe.
**terminal_reason: sole remaining residual is authority-gated by construction.**
